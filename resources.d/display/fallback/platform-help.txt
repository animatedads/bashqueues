Usage: queue platform [--json]

Emits local runtime/platform facts.

Notes:
- Windows support claim is WSL2-first.
- Native Windows shells are detected as future adapter/runtime tiers, not as worker-supported execution environments.
- Use --json for the queuebash.platform_facts.v1 contract.
