# dev merge-plan fixtures

The smoke test generates bounded root and nested patchset zips dynamically so the
fixture remains small while still proving root discovery, function collision
reporting, release-identity overlap handling, and human/JSON output.
