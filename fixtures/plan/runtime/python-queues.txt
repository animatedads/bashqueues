from celery import Celery
app.control.inspect().registered(); app.control.inspect().active(); app.control.inspect().scheduled()
from rq import Queue, Worker
Queue.all(); Worker.all()
from apscheduler.schedulers.background import BackgroundScheduler
scheduler.get_jobs()
