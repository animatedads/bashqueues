apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: batch-egress-only
spec:
  podSelector:
    matchLabels:
      role: batch
  policyTypes: [Egress]
  egress:
    - to:
        - namespaceSelector: {}
