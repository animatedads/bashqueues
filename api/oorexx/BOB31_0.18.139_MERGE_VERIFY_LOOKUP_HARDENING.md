# 0.18.139 BOB31 ooRexx API merge verification and Rexx lookup hardening

Verified Bob31's ooRexx JSON frontage survived the large 0.18.138 merge.  The api/oorexx lane is present, the platform JSON requirement remains unvendored, and the 0.18.137 constructor/argument-rule repair is carried forward.

Additional hardening in this drop:

- Uses explicit Directory~hasIndex checks before reading optional JSON/directory fields such as functions, function, priority, class, and retries.
- Keeps ooRexx omitted-argument handling based on arg() and symbolic uppercase placeholder normalisation rather than .nil assumptions.
- Keeps public lastError / last_error reporting for wrapper failures.
- Extends static guard coverage for the new Rexx lookup-hardening patterns.

Runtime ooRexx testing still requires a host with rexx installed.  Suggested host smoke:

```bash
cd api/oorexx
QUEUEBASH_SOURCE=../../queuebash.sh rexx examples/bq_json_frontage_demo.rex
```
