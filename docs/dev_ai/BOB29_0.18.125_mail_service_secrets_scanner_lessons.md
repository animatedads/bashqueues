# Bob29 0.18.125 AI lessons

- Non-interactive sourcing of queuebash.sh requires `QUEUEBASH_ALLOW_NONINTERACTIVE=1`; otherwise the script correctly returns before installing the `queue` function.
- The 0.18.124 merge already carried forward Bob29 `dns_service` and `cache_service`, so no rebase was needed.
- `secrets_scanner` must remain metadata-only: fixtures may describe scanner posture and finding state, but must never include raw secret values or alter secrets runtime/security controls.
