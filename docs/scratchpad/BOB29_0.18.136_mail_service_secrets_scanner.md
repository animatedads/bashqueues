# Bob29 0.18.136 mail_service + secrets_scanner carry-forward

Base: `bashqueues_0.18.135_BOB32_full_release_no_rto.zip`.

Verified SHA256 before work: `617b7ec7b7db59d90b90cd5fb479701dd7ae20612590ce820069f8ea6921549c`.

Finding: previous Bob29 families through `dns_service`/`cache_service` were present, but `mail_service` and `secrets_scanner` remained only as `next_candidates` in the service coverage registry. Rebased those two families as fixture-first provider/service contracts.

Boundary: no live calls, no mail sending, no mailbox reads, no live repository scans, no raw secret values, no credential rotation/revocation, no provisioning, and no queue runtime changes.
