# Event Stream explainability

The event_stream provider explains why a provider/service is suitable or unsuitable using bounded fixture facts: detection status, advisory metadata, policy notes, and governance flags.

Provider output is evidence for operators and policy gates. It is not an acceptance decision, not executable shell, and not a request to mutate the service.
