# RTO external component

This bashqueues release contains the queue-side RTO detection, wrapper, and `queue ask` advisory integration only.

The RTO application directory is intentionally not bundled in this full release package. Supply it separately by placing the RTO application at `./RTO` beside `queuebash.sh`, or by setting `QUEUEBASH_RTO_ROOT=/path/to/RTO`.
