# Bob29 AI lessons — 0.18.136

- Verified the incoming 0.18.135 archive hash before continuing.
- Treated the service coverage registry as advisory but verified the tree before assuming presence.
- `mail_service` and `secrets_scanner` were listed as next candidates but had no provider/test/doc trees, so the correct continuation was a carry-forward rebase rather than a new pair.
- Kept helpers fixture-only and normalized-JSON-only.
