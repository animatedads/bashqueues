# API Gateway legal and compliance posture

This package is advisory-only. It avoids live access, credential capture, secret export, package payload export, routing mutation, policy mutation, provisioning, or destructive operations.

The default policy example blocks lifecycle and mutation verbs and requires normalized JSON facts for any future extension.
